pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
PyYAML>=6.0,<7.0
pytest>=8.0,<9.0
fastapi>=0.110.0,<1.0.0
uvicorn>=0.29.0,<1.0.0
httpx>=0.27.0,<1.0.0
groq>=0.13.0,<1.0.0
mcp>=1.0.0,<2.0.0
python-dotenv>=1.0.0,<2.0.0
mangum>=0.17.0,<1.0.0
pymongo>=4.6.0,<5.0.0
# boto3 is required for DynamoDB storage and SSM Parameter Store lookups;
# it is pre-installed in the AWS Lambda Python runtime, so it is not listed
# here but must be installed locally for testing DynamoDBStorage.
